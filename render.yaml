services:
  - type: web
    name: clearpath-demo
    runtime: docker
    plan: free
    healthCheckPath: /checks/new
    envVars:
      - key: MATCHING_PROVIDER
        value: demo
      - key: SEED_DEMO_DATA
        value: true
      - key: DATABASE_URL
        value: sqlite:////tmp/clearpath.db
