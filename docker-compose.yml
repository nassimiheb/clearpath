services:
  clearpath:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - clearpath-data:/app/data
    restart: unless-stopped

volumes:
  clearpath-data:
